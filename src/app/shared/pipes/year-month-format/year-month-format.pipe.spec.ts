import { YearMonthFormatPipe } from './year-month-format.pipe';

describe('MonthPipe', () => {
  it('create an instance', () => {
    const pipe = new YearMonthFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
